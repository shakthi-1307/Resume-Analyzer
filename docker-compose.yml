version: '3.8'

services:
  app:
    build: .
    container_name: resume-analyzer
    ports:
      - "8000:8000"
    environment:
      # Set your Groq API key here or use Railway variables
      - GROQ_API_KEY=${GROQ_API_KEY}
    restart: unless-stopped
    volumes:
      - ./:/app
